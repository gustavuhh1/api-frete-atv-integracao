version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: postgres-api-frete
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: api_frete
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d api_frete"]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build: .
    container_name: node-api-frete
    ports:
      - "3000:3000"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    environment:
      - DATABASE_URL=postgresql://user:password@db:5432/api_frete?schema=public
    depends_on:
      db:
        condition: service_healthy

volumes:
  pgdata:
